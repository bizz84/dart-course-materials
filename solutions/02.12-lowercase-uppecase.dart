void main() {
  String title = 'Dart course';
  print(title);
  title = title.toUpperCase();
  print(title);
  title = title.toLowerCase();
  print(title);

  // Desired output:
  // Dart course
  // DART COURSE
  // dart course
}
